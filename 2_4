//4번 5글자 이상인 단어 배열하기


def solution(words):
	answer = ''
	for word in words:        #words 함수 안에 임의의 함수 word를 만든다.
		if len(word) >= 5:      #각 단어를 비교해가며 그 길이가 5보다 긴 경우에
			answer += word        #answer 에 그 단어를 넣는다
	if len(answer) < 1:       #answer에 아무것도 들어가지 않은 경우에는
		answer = 'empty'        #empty를 넣어줘서 비어있다는 사실을 알 수 있도록 한다.
	return answer
